<%- include('portals/header') %>

<div class="bg-dark">
    <a class="text-light d-block text-bolder text-center p-4" href="/memberdasboad">Go to dasboad</a>
<div class="bg-danger shadow rounded p-5" id="confirm">
    <h1 class="text-bolder text-center py-5 text-light">Go for deliver and earn moenry</h1>*
    <% if (confirmedorderfrmedel.length === 0) { %>
        <h1 class="fs-4 text-center bg-warning p-4 text-light">There is no order right mow. Please wait for an order created.</h1>
    <% } else { %>
    <% for ( let i = 0; i < confirmedorderfrmedel.length; i++ ) { %>
        <div class="bg-dark my-2 p-2 d-flex justify-content-center">
            <h3 class="w-75 text-light d-inline-block"><%=  confirmedorderfrmedel[i].name %></h3>
            <a class="py-1 rounded bg-danger shadow rounded px-4 fs-5" href="/submitorderpage/<%= confirmedorderfrmedel[i]._id %>/<%= userobj.useridlog %>">View</a>
        </div>
    <% } %>
    <% } %>

</div>
</div>


<%- include('portals/footer') %>
